---
phase: quick-260807-iz2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - monitor.py
  - test_monitor.py
  - README.md
autonomous: true
requirements: [QUICK-260807-iz2]

estimate:
  tokens: 62000
  raw_tokens: 62000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Replay of the 2026-08-07 13:14:20Z episode (session 6fff7d17 flips WORKING→WAITING with background_tasks_count 1, sibling 8ced4fe8 still WORKING in the same group) produces NO call to _notify — today it produces one"
    - "Replay of the 2026-08-07 13:28:53Z episode (bg count 1, sibling still working) likewise produces NO call to _notify"
    - "A held notification is RELEASED (fires exactly once) on the first tick where both gate conditions clear — i.e. the last group member finishing is what pages the user, matching the todo's 'notify only when all group members are done'"
    - "A held notification is DISCARDED without firing when the session itself goes back to WORKING (the real 13:14 case: the session resumed itself ~90s later), and the discard is recorded in the notifications log"
    - "A session whose group has no other member (or whose cwd is unknown) notifies exactly as it does today — the gate is inert for solo sessions, hookless/legacy rows, and any session dict without a cwd"
    - "Two sessions with cwd /workspace and /workspace/.claude/worktrees/prd-gsd under the same project label resolve to the SAME group key; two sessions both at cwd /workspace under DIFFERENT project labels resolve to different group keys (the devcontainer /workspace collision must not merge unrelated projects)"
    - "Every notification decision — sent AND suppressed — appends exactly one jsonl line to ~/.claude/notifications.log carrying timestamp, session_id, hostname, cwd, project, group, type (needs_input|stop|idle_prompt), the exact title/body that was (or would have been) displayed, and outcome + reason"
    - "A held notification logs its suppression ONCE per hold episode (not once per 5-second refresh tick) plus one resolution record when it fires, expires, resumes, or vanishes"
    - "The notifications log never crashes or stalls the refresh loop: an unwritable path, a full disk, or a read-only home degrades to a silent no-op"
    - "python3 -m unittest test_monitor.py stays green — all 165 pre-existing tests plus the new ones"
  prohibitions:
    - "MUST NOT weaken, delete, or loosen any existing assertion in Goal8_NotificationDebounce — the one-tick debounce, the green-blip discard, the vanish cleanup and the 60-second NOTIFY_MIN_WORK_SEC pin all stay exactly as they are; that class's fixture may only GROW"
    - "MUST NOT change how any session's STATE/colour is computed — background_tasks_count stays badge-only for state (D-03); this plan reads it purely as a notification gate input, and scan_state_files()'s verdict logic is untouched"
    - "MUST NOT log any prompt text, tool input/output, jsonl content, or assistant reply into notifications.log — the only free text permitted is the toast title/body the monitor itself composes from project label + alias + elapsed time (T-02-17 content-leak precedent)"
    - "MUST NOT use a session's cwd as a filesystem path: it arrives from a hook payload and is only ever string-normalised and compared, never opened, joined, globbed, or stat'ed"
    - "MUST NOT group sessions by display_name — display_name deliberately differs between duplicate containers of the same project (container_display_name's documented display-only rule); grouping identity uses the project label `name`"
    - "MUST NOT introduce a second read of monitor-state/, working-locks/, or any jsonl file — the gate consumes only the session dicts _check_transitions is already handed"
    - "MUST NOT add a new UI button, toggle widget, or window; the escape hatch is a config-file key only"
    - "MUST NOT install any npm/pip/cargo package"
  artifacts:
    - path: "monitor.py"
      provides: "notification_group_key(), gate_notification(), notification_type(), notification_text(), notification_record(), log_notification(), the NOTIFY_GATE_MAX_HOLD_SEC / NOTIFICATIONS_LOG / NOTIFICATIONS_LOG_MAX_BYTES / WORKTREE_MARKER constants, and the gated + logged _check_transitions fire path"
      contains: "notification_group_key"
    - path: "test_monitor.py"
      provides: "Goal25 (group key + gate), Goal26 (notifications jsonl log), Goal27 (hold/release/discard wiring incl. both real 2026-08-07 episodes replayed)"
      contains: "Goal27"
    - path: "README.md"
      provides: "One bullet stating notifications wait for the whole work group, and one Configuration line naming ~/.claude/notifications.log and the group_gate escape hatch"
      contains: "notifications.log"
  key_links:
    - "_check_transitions' single fire point (today `self._notify(session_display_text(s), self._pending_notify.pop(key), key=key)`) is the ONE place the gate and the log attach — refresh() already hands it the full render_sessions list, so every group sibling is in scope with zero extra I/O"
    - "scan_state_files() must copy the state record's `cwd` into the session dict it emits; the field exists in every file hooks/state-writer.sh writes but is currently dropped, so without this the group key has nothing to work with"
    - "notification_text() becomes the single composer of the toast title/body, called by BOTH _notify() and notification_record(), so the logged text is provably the displayed text"
---

<objective>
Stop the monitor from paging the user when ONE session of a multi-session work group finishes a turn, and make every notification decision auditable on disk.

Purpose: on 2026-08-07 the monitor fired twice (13:14:20Z and 13:28:53Z) on intermediate checkpoints of session 6fff7d17 while its sibling 8ced4fe8 was still working; both sessions were the same job on the same repo across two containers. The user reads such a toast as "work finished". Both gate conditions the todo specifies would have suppressed both notifications. The companion jsonl log exists so the gate can be validated in the field — including the notifications it suppresses — before it is trusted.

Output: a group gate and an append-only notifications log, both attached at the monitor's single notification choke point, plus unit coverage that replays the two real episodes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/todos/pending/2026-08-07-notifica-gated-sul-gruppo-solo-quando-tutte-le-sessioni-term.md
@.planning/todos/pending/2026-08-07-log-su-file-di-tutte-le-notifiche-inviate.md

Orientation already established — do not re-derive it:

- `monitor.py` ~2900 lines. The notification choke point is `MonitorApp._check_transitions` (defined ~line 1856); its only fire statement is the `elif key in self._pending_notify:` branch (~line 1889) calling `self._notify(...)`. `refresh()` calls `_check_transitions(render_sessions)` (~line 2218) with the complete rendered session list, so all group siblings are already in the argument.
- `scan_state_files()` builds each session dict at ~lines 808-826. It already carries `key`, `name`, `session_id`, `status`, `hostname`, `background_tasks_count`, `display_name`, `alias_key` — but NOT `cwd`, even though `hooks/state-writer.sh` writes `cwd` into every state record (its jq object, ~line 187).
- Legacy `scan()` rows (hookless containers, carried through the bridge with `legacy_origin`) have `working_locked` but no `cwd` and no `background_tasks_count`.
- `_notify()` (~line 2018) composes `toast_title` = `f"Claude ready — {shown}"` and `toast_body` = `f"Waiting for your input after {elapsed_human} of work."`, where `shown` appends the session alias when set.
- Module-level, tkinter-free helpers are this codebase's established shape for anything unit-tested (`container_display_name`, `session_display_name`, `bg_badge_text`, `session_display_text`). Tests repoint module-level path constants (`STATE_DIR`, `PROJECTS_DIR`) — see `StateFileTestBase`.
- `hooks/event-logger.sh` writes `~/.claude/hook-events.log` as jsonl with a 5 MB truncate-and-marker size guard; the new log mirrors that shape and threshold.
- Test suite: `python3 -m unittest test_monitor.py`, 165 tests, ~0.1s, tkinter stubbed at import. Goal numbering is in use up to Goal24.
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Group identity + the two gate conditions, proven end to end on the real 13:14:20Z episode</name>
  <files>monitor.py, test_monitor.py</files>
  <read_first>monitor.py lines 800-830 (the scan_state_files session dict), lines 1856-1898 (_check_transitions), lines 55-125 (the constants block); test_monitor.py lines 1062-1135 (Goal8_NotificationDebounce) and 1141-1192 (StateFileTestBase)</read_first>
  <behavior>
    Thin end-to-end slice: one real episode travels from a state record on disk, through the session dict, through the group key, through the gate, to a suppressed notification decision. No log yet (Task 2), no hold/release bookkeeping yet (Task 3) — this task's wiring may simply drop a gated notification.

    - Test 1 (group key, worktree fold): a session dict with name "nursy_app" and cwd "/workspace/.claude/worktrees/prd-gsd" and one with name "nursy_app" and cwd "/workspace" produce the SAME group key. A cwd deeper inside a worktree ("/workspace/.claude/worktrees/prd-gsd/src") folds to the same key too.
    - Test 2 (group key, collision guard): name "nursy_app" cwd "/workspace" and name "dev-tools" cwd "/workspace" produce DIFFERENT group keys — every devcontainer mounts its project at /workspace, so cwd alone must never merge unrelated projects.
    - Test 3 (group key, unknown cwd): a session dict with no cwd, an empty cwd, or a non-string cwd produces the empty string, meaning "ungrouped".
    - Test 4 (group key, separators): a Windows-style cwd with backslashes and a trailing separator normalises to the same key as its forward-slash form.
    - Test 5 (gate condition a): a session with background_tasks_count 1 and NO siblings is refused, reason "background_tasks"; the same session with count 0 is allowed; with the field absent or non-numeric it is allowed (unknown must never suppress, or hookless rows would go silent).
    - Test 6 (gate condition b): a session whose group contains another session with status "WORKING" is refused, reason "group_working", and the refusal detail names the blocking session's key; the same session is allowed once that sibling reads WAITING. A sibling in a DIFFERENT group never blocks. A legacy sibling in the same group with working_locked True blocks exactly like a WORKING one.
    - Test 7 (self-exclusion): a session is never blocked by its own presence in the list.
    - Test 8 (episode replay, end to end): write two state files into a temp STATE_DIR reproducing 2026-08-07 — session "6fff7d17" state waiting, last_event Stop, background_tasks_count 1, hostname "8d5f9694f1de", cwd "/workspace"; session "8ced4fe8" state working, hostname "4353e1441213", cwd "/workspace/.claude/worktrees/prd-gsd" — both labelled "nursy_app". Feed scan_state_files' output through gate_notification for 6fff7d17 and assert it is refused. Assert also that the session dict scan_state_files emitted carries cwd at all.
    - Test 9 (regression): scan_state_files' existing verdict, colour, rank and staleness behaviour is unchanged by the added field — the pre-existing Goal9/Goal10/Goal11/Goal24 classes stay green untouched.
  </behavior>
  <action>
Add `WORKTREE_MARKER = "/.claude/worktrees/"` to the constants block near STATE_TOMBSTONE_SUFFIX, with a comment naming the real 2026-08-07 case it exists for.

In `scan_state_files()`, add `"cwd"` to the session dict it appends, read defensively from the record the same way `hostname` already is (a non-string or missing value becomes None). Change nothing else in that function — no verdict, colour, rank, staleness or bridge behaviour moves.

Add two module-level, tkinter-free functions next to `session_display_text` / `bg_badge_text`, matching this file's docstring density (state WHY, not what):

`notification_group_key(s)` — returns the work-group identity of one session dict as a string. Resolve cwd from the dict; if it is not a non-empty string, return "" (ungrouped: a solo group of one, which the gate treats as never blocking, preserving today's behaviour for legacy/hookless rows). Otherwise normalise: backslashes to forward slashes, strip trailing separators, and if WORKTREE_MARKER appears in the path truncate everything from the marker onward so a worktree checkout folds onto its main repo root. Combine with the session's project label `name` (NOT display_name — document why, citing container_display_name's display-only rule) into one string using a "::" separator. Document the deliberate asymmetry: the label is what stops two unrelated devcontainers, both mounted at /workspace, from being treated as one group, and the cost is that a sibling launched under a different project label simply stays ungrouped — the safe direction, since over-grouping would silence a genuine "this session needs you now".

`gate_notification(session, sessions)` — returns a three-tuple of (allowed: bool, reason: str, detail: dict). Refuse with reason "background_tasks" when the session's background_tasks_count is a number greater than zero (an absent, None or non-numeric value is unknown and must NOT refuse). Otherwise resolve the group key; when it is non-empty, refuse with reason "group_working" if any OTHER entry in `sessions` (different `key`) has the same group key and either reads status "WORKING" or carries working_locked True. Allowed returns reason "". The detail dict carries the group key, the background_tasks_count that was examined, and for a group refusal the list of blocking session keys — identifiers only, never any text a session produced. Note in the docstring that reading background_tasks_count here is a NOTIFICATION gate, not a state input, so D-03's badge-only rule for STATE is untouched.

Wire the gate at `_check_transitions`' single fire statement: resolve the gate before calling `self._notify(...)` and skip the call when it refuses. Keep the existing pop-and-forget semantics for now — Task 3 replaces this with the hold/release behaviour. Guard the whole gate behind `self.config.get("group_gate", True)` read through a defensive accessor so a Fake test app without a `config` attribute is unaffected; document that this is a config-file-only escape hatch with no UI surface, present so the gate can be switched off in the field without a code change.

In test_monitor.py add class `Goal25_GroupGatedNotifications` covering the behaviours above, following the file's existing idioms: derive from StateFileTestBase for the episode replay, use plain unittest.TestCase for the pure-function tests, and name each test after the user-facing guarantee.
  </action>
  <verify>
    <automated>cd /workspace &amp;&amp; python3 -m unittest test_monitor.Goal25_GroupGatedNotifications -v &amp;&amp; python3 -m unittest test_monitor.py 2>&amp;1 | tail -3</automated>
  </verify>
  <done>Goal25 passes; the full suite passes with no pre-existing test modified; `grep -n '"cwd"' monitor.py` shows the field propagating out of scan_state_files; replaying the 13:14:20Z pair through gate_notification returns allowed=False.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Append-only notifications.log — one jsonl record per decision, sent or suppressed</name>
  <files>monitor.py, test_monitor.py</files>
  <read_first>monitor.py lines 2018-2045 (_notify's title/body composition); hooks/event-logger.sh lines 40-60 and 114-146 (the jsonl + size-guard idiom this mirrors)</read_first>
  <behavior>
    - Test 1 (round trip): logging one record appends exactly one line to NOTIFICATIONS_LOG; the line parses as JSON; a second call appends a second line and leaves the first byte-identical.
    - Test 2 (record shape): a record built from a state-engine session dict carries ts, ts_ms, session_id, key, hostname, cwd, project, group, type, state, title, body, elapsed_sec, background_tasks_count, outcome and reason.
    - Test 3 (type mapping): state "waiting" maps to "stop", "needs_input" to "needs_input", "idle" to "idle_prompt", and anything missing or unrecognised to "unknown" — never a crash.
    - Test 4 (displayed text is the logged text): notification_text produces the exact title and body strings _notify displays today, including the alias suffix when an alias is set and its absence when not.
    - Test 5 (never crashes the loop): with NOTIFICATIONS_LOG pointed at an unwritable location, logging returns without raising and the caller continues.
    - Test 6 (size guard): a log already larger than NOTIFICATIONS_LOG_MAX_BYTES is truncated before the next append and a `_truncated` marker line precedes it; a log under the threshold is never touched.
    - Test 7 (no content leak): a session dict carrying prompt-like or tool-like fields yields a record whose serialised form contains none of them — only the identity fields and the monitor-composed title/body.
  </behavior>
  <action>
Add to the constants block, beside the existing log/path constants: `NOTIFICATIONS_LOG = Path.home() / ".claude" / "notifications.log"` and `NOTIFICATIONS_LOG_MAX_BYTES = 5 * 1024 * 1024`. Document the placement decision: the todo proposed monitor-state/, but that directory is the hook writer's per-session state store which scan_state_files() sweeps and prunes; a monitor-written append-only log belongs beside hook-events.log at the ~/.claude root, at the same tier and in the same format. Module-level so tests repoint it exactly as they repoint STATE_DIR. Also add `NOTIFY_GATE_MAX_HOLD_SEC = 1800` here (Task 3 consumes it) with a comment explaining the cap: a held toast released 40 minutes late would misinform, and dropping it costs the user nothing because the overlay keeps showing the session green the whole time — precedent, the 900-second BG_PIN_MAX_SEC cap.

Extract the toast text composition out of `_notify()` into a module-level `notification_text(label, elapsed_sec, alias="")` returning the (title, body) pair, byte-identical to what `_notify` builds today including the minutes/seconds humanisation and the alias suffix rule. Call it from `_notify()` so exactly one composer exists and the logged text is provably the displayed text.

Add `notification_type(state)` — maps the state record's own value to the todo's vocabulary: waiting to "stop", needs_input to "needs_input", idle to "idle_prompt", anything else (including None and non-strings) to "unknown".

Add `notification_record(s, elapsed_sec, alias, outcome, reason, **extra)` — builds the flat dict to be logged: an ISO-8601 UTC ts and an integer ts_ms, session_id (falling back to the row key when the row has no session_id), key, hostname, cwd, project (the `name` label), group (notification_group_key), type, state, title and body from notification_text, elapsed_sec, background_tasks_count, outcome and reason, merging any extras the caller supplies (held_sec, blocked_by, group detail). Enumerate the fields explicitly — never dump the session dict — and say in the docstring that this explicit allow-list is what keeps prompt/tool content out of the log (T-02-17 precedent).

Add `log_notification(record)` — serialises one compact JSON line and appends it to NOTIFICATIONS_LOG, creating the parent directory when missing. Before appending, if the file exists and exceeds NOTIFICATIONS_LOG_MAX_BYTES, truncate it and write a `_truncated` marker line first, mirroring event-logger.sh's guard and threshold. The whole body is best-effort: any exception is swallowed and the function returns None, because this runs inside the 5-second refresh loop and a full disk must never stall or crash it. Note that no file lock is needed here (unlike the hook script's flock) because the monitor holds a process singleton, so there is exactly one writer.

In test_monitor.py add class `Goal26_NotificationsLog` with its own setUp/tearDown repointing `monitor.NOTIFICATIONS_LOG` into a TemporaryDirectory, covering the behaviours above.
  </action>
  <verify>
    <automated>cd /workspace &amp;&amp; python3 -m unittest test_monitor.Goal26_NotificationsLog -v &amp;&amp; python3 -m unittest test_monitor.py 2>&amp;1 | tail -3</automated>
  </verify>
  <done>Goal26 passes; the full suite passes; `_notify` composes its title/body only via notification_text (no second f-string builds a toast title anywhere in monitor.py).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Hold-release-discard at the choke point, both outcomes logged, README note</name>
  <files>monitor.py, test_monitor.py, README.md</files>
  <read_first>monitor.py lines 1856-1898 (the version Task 1 left) and 1500-1510 (the per-session transition dicts initialised in __init__)</read_first>
  <behavior>
    - Test 1 (13:14:20Z replay, end to end through _check_transitions): a session that worked long enough flips to WAITING with background_tasks_count 1 while its group sibling reads WORKING — across two WAITING ticks, _notify is never called, and exactly ONE suppressed record is logged, not one per tick.
    - Test 2 (13:28:53Z replay): same shape with the sibling as the only blocker (bg count 0) — no notification, reason "group_working", blocked_by naming the sibling.
    - Test 3 (release): while the notification is held, the sibling flips to WAITING and the held session's own bg count reads 0 — on the next tick _notify fires exactly once, and a "sent" record is logged carrying held_sec. A further tick fires nothing more.
    - Test 4 (discard on self-resume, the real 13:14 outcome): while held, the session itself returns to WORKING — _notify is never called, the pending entry and the hold are both cleared, and one suppressed record with reason "session_resumed" is logged.
    - Test 5 (discard on vanish): while held, the session disappears from the list — no notification, state cleared, one suppressed record with reason "session_gone" logged from the snapshot taken when the hold opened.
    - Test 6 (hold cap): while held past NOTIFY_GATE_MAX_HOLD_SEC, the notification is dropped, not fired, and one suppressed record with reason "hold_expired" is logged.
    - Test 7 (reason change reopens): a hold that starts as "background_tasks" and later becomes "group_working" logs the second reason once, and still only once while it persists.
    - Test 8 (solo session unaffected): a session with no group siblings and no background tasks notifies on the second WAITING tick exactly as today, and logs one "sent" record.
    - Test 9 (escape hatch): with the group_gate config key set false, the 13:14:20Z replay notifies (today's behaviour) and still logs a "sent" record — the log is independent of the gate.
    - Test 10 (existing debounce intact): Goal8_NotificationDebounce's four tests pass unchanged in substance.
  </behavior>
  <action>
Replace Task 1's provisional pop-and-drop with hold semantics at the same single fire point. Add `self._notify_hold: dict[str, dict]` beside `_pending_notify` in `__init__`, documented as the open-hold register: one entry per session whose notification is armed but gated, holding the tick it opened at, the reason currently blocking it, and a snapshot of the record fields captured when the hold opened (so a session that later vanishes can still be logged without its dict).

At the fire point: resolve the gate. When allowed, clear any hold, fire `self._notify(...)` unchanged, and log one record with outcome "sent" and reason "" (or "gate_cleared" when a hold was open, carrying held_sec). When refused, do NOT pop the pending entry — open or update the hold, and log a suppressed record ONLY when the hold opens or its reason changes, so a 5-second refresh loop cannot flood the file (same episode-collapse idiom as the divergence filter's ticks counter). If the hold's age reaches NOTIFY_GATE_MAX_HOLD_SEC, pop both pending and hold and log outcome "suppressed" reason "hold_expired".

In the WORKING branch, where `_pending_notify` is already popped, also pop the hold and, when one was open, log outcome "suppressed" reason "session_resumed" with held_sec. In the vanished-session cleanup loop, do the same with reason "session_gone", building the record from the hold's snapshot.

Keep the alias resolution consistent with `_notify`'s (the same `self._session_aliases.get(self._alias_key(key), "")` expression) and pass it to notification_record, so the logged title matches the displayed title including the alias.

Update `Goal8_NotificationDebounce._fake_app` to carry the attributes the enlarged `_check_transitions` touches — an empty `_notify_hold`, an empty `_session_aliases`, an identity `_alias_key`, and a `config` dict — and give the class setUp/tearDown that repoints `monitor.NOTIFICATIONS_LOG` into a TemporaryDirectory so the suite never writes to the developer's real home. Its existing assertions must not be relaxed, reworded away, or deleted.

Add class `Goal27_NotificationGateWiring` covering the behaviours above, driving `monitor.MonitorApp._check_transitions` through a Fake app in Goal8's style and asserting on both the captured `_notify` calls and the parsed jsonl lines.

README: add one bullet under "What it does" stating that when several sessions work the same project the monitor waits for the last one before paging, and one line under "Configuration" naming `~/.claude/notifications.log` (every notification, sent and suppressed, as jsonl) and the config key that disables the gate. Two short additions, no restructuring.
  </action>
  <verify>
    <automated>cd /workspace &amp;&amp; python3 -m unittest test_monitor.Goal27_NotificationGateWiring test_monitor.Goal8_NotificationDebounce -v &amp;&amp; python3 -m unittest test_monitor.py 2>&amp;1 | tail -3</automated>
  </verify>
  <done>Goal27 and Goal8 pass; the full suite passes with at least 165 tests; both 2026-08-07 episodes replay to zero _notify calls with one suppressed jsonl record each; the release path fires exactly once; README names notifications.log.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| hook payload → state file → monitor | `cwd`, `hostname` and `background_tasks_count` originate in Claude Code's hook payload, are written by hooks/state-writer.sh, and are consumed here as untyped JSON |
| monitor → ~/.claude/notifications.log | the monitor writes attacker-influenceable field values into a persistent file under the user's home |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-iz2-01 | Information disclosure | notification_record() → notifications.log | medium | mitigate | Explicit field allow-list; the only free text is the monitor-composed title/body (project label + alias + elapsed). Session content, prompts, tool I/O and jsonl tails are never read by this path (T-02-17 precedent). Verified by Goal26 Test 7. |
| T-iz2-02 | Denial of service | log_notification() unbounded growth | medium | mitigate | 5 MB truncate-and-marker guard mirroring hooks/event-logger.sh, plus per-episode (not per-tick) suppression logging so a stuck hold cannot write 12 lines a minute. Verified by Goal26 Test 6 and Goal27 Test 1. |
| T-iz2-03 | Tampering | hostile `cwd` value used to build a group key | high | mitigate | `cwd` is only string-normalised and compared — never joined, opened, globbed or stat'ed. `..`, absolute paths and traversal sequences are inert because no filesystem operation consumes the value. Stated as a plan prohibition. |
| T-iz2-04 | Denial of service | log write failure inside the 5s refresh loop | high | mitigate | log_notification() is wholly best-effort: any exception is swallowed and the caller continues. Verified by Goal26 Test 5. |
| T-iz2-05 | Denial of service | over-broad group key silences a genuine "needs you now" | high | mitigate | Group identity requires BOTH the project label and the normalised root, so unrelated devcontainers at /workspace never merge; ungrouped is the default for any unknown cwd; a held notification is capped at NOTIFY_GATE_MAX_HOLD_SEC and the overlay keeps rendering the session green regardless; `group_gate: false` disables the gate without a code change. Verified by Goal25 Tests 2/3 and Goal27 Tests 6/8/9. |
| T-iz2-SC | Tampering | npm/pip/cargo installs | n/a | accept | No packages are installed — stdlib only (json, pathlib, time), all already imported by monitor.py. |
</threat_model>

<verification>
- `python3 -m unittest test_monitor.py` green, count ≥ 165 + the new classes, no pre-existing assertion changed.
- `git diff` touches only monitor.py, test_monitor.py, README.md.
- The two 2026-08-07 episodes, replayed through `_check_transitions`, produce zero `_notify` calls and one suppressed jsonl record each.
- Releasing the gate fires the held notification exactly once; a self-resume discards it.
- Content-leak coverage is assertion-based, not grep-based (Goal26 Test 7): a session dict seeded with session-content-shaped fields yields a serialised record containing none of their values.
</verification>

<success_criteria>
A single session's Stop no longer pages the user while a sibling of the same work group is still working; the page arrives when the last member stops, or not at all if the session resumes itself. Every decision — sent or suppressed, with its reason — is one jsonl line in `~/.claude/notifications.log`, so the gate can be audited against the field before it is trusted.
</success_criteria>

<output>
Create `.planning/quick/260807-iz2-group-gated-notifications-sent-notificat/260807-iz2-SUMMARY.md` when done
</output>
