---
phase: 03-flip-to-default-cleanup
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - monitor.py
  - shell_tracker.py
  - hooks/state-writer.sh
  - test_monitor.py
  - test_state_writer.py
autonomous: true
requirements: [FLIP-01, FLIP-02]
estimate:
  tokens: 72000
  raw_tokens: 72000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A live background shell never by itself makes a session render WORKING; it only lights the single unified badge, whose count comes from the hook-captured background_tasks_count (D-03)."
    - "The overlay shows exactly one badge per session row and per compact chip — the two-badge pair is gone."
    - "An Esc-interrupted turn recovers from WORKING to WAITING in roughly 90 seconds rather than waiting out the 600s heartbeat window (D-02a, UAT C1)."
    - "A long-running Monitor tool, foreground subagent or slow Bash call keeps its session pinned WORKING past the 600s heartbeat window instead of falsely flipping to WAITING and notifying (D-02b, divergence class 6)."
    - "A session running in a container with no hooks installed stays visible in the overlay via per-session legacy parsing (D-02c / ENG-05)."
    - "A session whose SessionEnd hook fired never reappears in the overlay, even while its jsonl row is still fresh enough for legacy parsing to see it (D-06)."
    - "Leftover atomic-write temp files in the state directory are swept automatically instead of accumulating on the shared mount (D-08)."
    - "Interruption and parallelism are bounded: a state file that vanishes between the directory listing and the read drops only that session, and the temp sweep only removes files older than STATE_PRUNE_AGE_SEC so an in-flight write is never destroyed (edge: FLIP-01/concurrency)."
    - "The door to remote aggregation stays open without any aggregation work landing: every state record still carries its `hostname` field, and state files remain per-machine under the state directory, so a future aggregator need only transport them (D-11)."
  prohibitions:
    - statement: "MUST NOT reintroduce jsonl scanning as a badge source — no badge, count or icon may be derived from parsing session transcript content once the hook-captured count is the source."
      status: unverified
      flagged: true
    - statement: "MUST NOT delete or weaken a test that asserts a user-facing guarantee without an equivalent assertion landing in the same phase; a smaller green suite must never be presented as equivalent coverage."
      status: unverified
      flagged: true
  artifacts:
    - path: "monitor.py"
      provides: "Trimmed in-file agent-activity peek and the two new staleness fallbacks"
      contains: "agent_activity"
    - path: "hooks/state-writer.sh"
      provides: "SessionEnd tombstone marker alongside the existing state-file removal"
      contains: "SessionEnd"
    - path: "shell_tracker.py"
      provides: "DELETED — badge parsing ends at the flip (D-03)"
  key_links:
    - from: "monitor.py scan()"
      to: "monitor.py scan_state_files()"
      via: "legacy_sessions rows carry working_locked, mtime and agent_activity, read by the staleness block as fallback evidence"
      pattern: "agent_activity"
    - from: "hooks/state-writer.sh SessionEnd branch"
      to: "monitor.py scan_state_files()"
      via: "tombstone marker file suppresses the legacy_origin bridge for a cleanly-ended session"
      pattern: "ended"
---

<objective>
Complete FLIP-01's engine work: delete the last permanent jsonl badge parser, and implement the two genuinely-new fallback behaviours plus the ghost suppression the flip requires.

Purpose: plan 03-01 proved the seam. This plan makes the surviving `scan()` do exactly three jobs and no more (D-02a/b/c), retires `shell_tracker.py` and the two-badge pair (D-03), and closes the two behaviour gaps the flip would otherwise open — divergence class 6 regressing into false notifications, and cleanly-ended sessions resurrecting as ghost rows (D-06).

Output: `monitor.py` with a trimmed in-file activity peek and an extended staleness block; `shell_tracker.py` deleted; `hooks/state-writer.sh` writing a SessionEnd tombstone; tests for all of it.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-flip-to-default-cleanup/03-CONTEXT.md
@.planning/phases/03-flip-to-default-cleanup/03-RESEARCH.md
@.planning/phases/03-flip-to-default-cleanup/03-PATTERNS.md
@.planning/phases/03-flip-to-default-cleanup/03-01-SUMMARY.md
</context>

## Artifacts this phase produces (plan 03-02 slice)

**Deleted:**
- the file `shell_tracker.py` in full, and every import of it from `monitor.py`
- `has_active_shells()` and the three regex patterns exclusive to it (the background-start pattern, the KillShell pattern, and the older task-wrapper status pattern)
- row fields `bg`, `monitors`, `agents` on both engines' rows
- the second badge widget on session rows and compact chips (the two-badge pair becomes one)
- `test_monitor.py` classes `Goal2h_BgShellBadgeOnly` and `Goal2i_ManualBgShellDetected`

**Added to `monitor.py`:**
- a module-private single-read activity peek that reports whether a Monitor tool, foreground Agent or async agent is in flight for a given jsonl (replacing three separate whole-file reads with one)
- `bg_badge_text(count)` — module-level, tkinter-free badge-text helper
- `STATE_TOMBSTONE_SUFFIX` constant and tombstone-aware bridging inside `scan_state_files()`
- stale temp-file sweep inside `scan_state_files()`'s existing prune loop
- D-02a early-recovery and D-02b pin branches inside `scan_state_files()`'s staleness block

**Changed:**
- `scan()` emits `agent_activity` (bool) and `background_tasks_count` (absent for legacy rows) in place of `bg`/`monitors`/`agents`
- `hooks/state-writer.sh` SessionEnd branch writes a tombstone marker in addition to removing the state file

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Retire shell_tracker and unify the badge on the hook-captured count</name>
  <reversibility rating="costly">D-03: deleting a 247-line module, three row fields and a rendering widget; undo means restoring the module, its 12 regexes, its call sites and two test classes across monitor.py, test_monitor.py and the Tk row/chip builders.</reversibility>
  <files>monitor.py, shell_tracker.py, test_monitor.py</files>
  <read_first>
    - `/workspace/shell_tracker.py` (full file — the four public functions, the twelve regex patterns, and which of them are shared between functions)
    - `/workspace/monitor.py` lines 20-35 (the import block naming the four shell_tracker functions)
    - `/workspace/monitor.py` lines 922-1082 (`scan()` in full — the four call sites, the WORKING OR-chain, and the row dict it builds)
    - `/workspace/monitor.py` lines 660-700 (`scan_state_files()`'s row dict, which currently hardcodes `monitors: 0` and `agents: 0` and derives `bg` from the state record's count)
    - `/workspace/monitor.py` lines 1655-1675 (the row-widget key tuple that names the badge widgets)
    - `/workspace/monitor.py` lines 2200-2230 and 2255-2280 (the standard-row and compact-chip badge update blocks)
    - `/workspace/monitor.py` lines 2300-2365 (the row and chip builders that create the two badge labels)
    - `/workspace/test_monitor.py` lines 333-560 (`Goal2_ClaudeActivelyWorking`, `Goal2h_BgShellBadgeOnly`, `Goal2i_ManualBgShellDetected` — which assertions are shell_tracker-only and which survive)
    - `/workspace/test_monitor.py` lines 888-925 (`Goal4_CertainlyWorkingMatrix`, to confirm it does not depend on the deleted counters)
    - `/workspace/.planning/TEST-MATRIX.md` section 4 rev.2 (the locked badge-only rule)
  </read_first>
  <behavior>
    - Test 1: `bg_badge_text(0)` and `bg_badge_text(None)` both return the empty string.
    - Test 2: `bg_badge_text(1)` returns a single badge glyph with no number; `bg_badge_text(3)` returns the same glyph followed by `3`.
    - Test 3: a state-file record written with `background_tasks_count=2` produces a row whose badge text shows the count, while the row's `status` is whatever the record's `state` maps to — the count never changes the status.
    - Test 4: a state-file record written with `state="waiting"` and `background_tasks_count=5` produces `status == "WAITING"` (bg count does not pin WORKING).
    - Test 5: a jsonl fixture whose tail shows an unmatched Monitor tool_use yields a `scan()` row with `agent_activity` True; a fixture with a matched TaskStop for the same task id yields False.
    - Test 6: a jsonl fixture whose tail shows an unmatched foreground Agent tool_use yields `agent_activity` True; adding the matching tool_result flips it to False.
    - Test 7: `import shell_tracker` raises `ModuleNotFoundError`, and `monitor` imports cleanly without it.
  </behavior>
  <action>
Delete `shell_tracker.py` and absorb only the Monitor/agent-activity detection the D-02b pin needs, as a trimmed in-file peek.

This resolves RESEARCH.md Open Question 1 in the direction the orchestrator bound: D-02b's "active Monitor/agent evidence" is RETAINED (02-DIVERGENCE-REVIEW carry-forward #1 names it explicitly, and the hybrid model keeps targeted jsonl peeks by design), while D-03's deletion of the module and of all badge parsing still stands. Do NOT adopt RESEARCH.md's assumption A2 alternative of working-lock evidence alone.

**The peek.** Add one module-private helper to `monitor.py` that opens a jsonl once, reads the same trailing window `shell_tracker` used, and returns a single boolean: True when at least one Monitor tool task, foreground Agent call or async agent launch is started-but-not-terminated in that window. Port the patterns those three detections require — the Monitor tool_use pattern, the Monitor tool_result task-id pattern, the Monitor persistence-flag pattern, the TaskStop pattern, the Monitor-timeout pattern, the task-notification task-id pattern, the Agent tool_use pattern, the generic tool_result-id pattern, the async-agent launch pattern, the task-notification status pattern, and the terminal-status set. Do NOT port the background-shell start pattern, the KillShell pattern, the older task-wrapper status pattern, or `has_active_shells` — those exist solely to drive the badge, which is now hook-native. The peek must read the file exactly once and derive all three signals from that one buffer; the module it replaces performed three independent whole-file reads per session per tick. Keep the existing byte-level, unescaped-quote anchoring in each ported pattern — it is what prevents a grep/cat of a jsonl inside a transcript from producing false positives — and keep the OSError-returns-safe-default behaviour.

**`scan()`.** Replace the three counter calls and the `has_active_shells` call with one call to the new peek, assigned to a local `agent_activity`. In the WORKING OR-chain, the three separate count comparisons collapse to that one boolean; the rest of the chain (`is_certainly_working`, `fresh_user_tail`, `working_locked`, and the `auq_locked` override that precedes it) is unchanged. In the row dict, drop the `bg`, `monitors` and `agents` fields and add `agent_activity`. Leave `auq_locked` and the `AUQ_LOCK_DIR` read exactly as they are — auq-lock retirement is gated separately in plan 03-03 and must not be bundled here (RESEARCH.md Pitfall 3).

**`scan_state_files()`.** Drop the `bg`, `monitors` and `agents` fields from its row dict too; the already-present `background_tasks_count` field is the badge's only source. Legacy-bridged rows carry no such field and therefore show no badge — that is the intended end state of D-03, not a gap.

**Rendering.** Add a module-level `bg_badge_text(count)` helper next to `container_display_name`/`session_display_name` — same rationale, display-only and tkinter-free so it is unit-testable without constructing `MonitorApp`. It returns the empty string for a falsy/absent/zero count, the badge glyph alone for exactly 1, and the glyph followed by the number for greater counts. Reuse the teal `◉` glyph and the existing teal foreground colour that the current monitor badge uses; the gold gear badge and its label widget are removed on both the standard row and the compact chip, so each row/chip builder creates one badge label instead of two. Collapse the two widget keys into one in the row/chip dicts and in the widget-key tuple that styles them, and replace both two-line badge update blocks with a single update driven by `bg_badge_text(s.get("background_tasks_count"))`.

**Tests.** Delete `Goal2h_BgShellBadgeOnly` and `Goal2i_ManualBgShellDetected` — both exist only to exercise the deleted background-shell parser. Their user-facing guarantee ("a live background shell never pins WORKING by itself") does not disappear with them: re-assert it on state-file fixtures per Tests 3 and 4 above, added to the `StateFileTestBase` family. Audit `Goal2_ClaudeActivelyWorking` and `Goal4_CertainlyWorkingMatrix` for assertions that reference the removed counters or row fields and repoint them at `agent_activity`; keep every assertion whose subject survives. Add the peek's own cases (Tests 5 and 6) using the existing jsonl fixture helpers in `MonitorTestBase`.
  </action>
  <verify>
    <automated>cd /workspace && python3 -m unittest test_monitor.py test_state_writer.py 2>&1 | tail -3 && test ! -f shell_tracker.py && python3 -c "import sys, types; sys.modules.setdefault('tkinter', types.ModuleType('tkinter')); import monitor; assert not hasattr(monitor,'has_active_shells'); assert not hasattr(monitor,'count_active_monitors'); assert not hasattr(monitor,'count_active_agents'); assert monitor.bg_badge_text(0)=='' and monitor.bg_badge_text(None)=='' and len(monitor.bg_badge_text(1))==1 and monitor.bg_badge_text(3).endswith('3'); print('BADGE OK')" && python3 -c "
try:
    import shell_tracker
except ModuleNotFoundError:
    print('MODULE GONE OK')
else:
    raise SystemExit('shell tracker module still importable')"</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -f shell_tracker.py` succeeds and `import shell_tracker` raises `ModuleNotFoundError`.
    - `hasattr(monitor, n)` is False for `has_active_shells`, `count_active_monitors`, `count_active_agents`, `count_active_async_agents`.
    - `monitor.bg_badge_text(0) == ""`, `monitor.bg_badge_text(None) == ""`, `len(monitor.bg_badge_text(1)) == 1`, `monitor.bg_badge_text(3)` ends with `"3"`.
    - A `scan_state_files()` row from a record with `state="waiting", background_tasks_count=5` has `status == "WAITING"`, and `bg_badge_text(row["background_tasks_count"])` is non-empty.
    - Every row dict returned by `scan()` and by `scan_state_files()` contains no `bg`, `monitors` or `agents` key (assert via `set(row) & {"bg","monitors","agents"} == set()`).
    - `scan()` rows contain an `agent_activity` key whose value is a bool.
    - A jsonl fixture with an unmatched Monitor tool_use gives `agent_activity is True`; adding a TaskStop for that task id gives False. Same shape for a foreground Agent tool_use with and without its matching tool_result.
    - `grep -c "class Goal2h_\|class Goal2i_" test_monitor.py` outputs `0`.
    - `python3 -m unittest test_monitor.py test_state_writer.py` reports OK.
  </acceptance_criteria>
  <done>The badge parser module is gone, one unified badge renders from the hook-captured count, `scan()` reports a single agent-activity boolean from a single-read peek, and the badge-only guarantee is re-asserted on state-file fixtures.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The two staleness fallbacks — Esc recovery and the hook-silence pin</name>
  <reversibility rating="reversible">Both branches live inside one staleness block in `scan_state_files()`; removing them restores the flat 600s/90s behaviour with no data or contract change.</reversibility>
  <files>monitor.py, test_monitor.py</files>
  <read_first>
    - `/workspace/monitor.py` lines 660-715 (`scan_state_files()`'s staleness-recovery block and the legacy-merge tail — the exact block being extended)
    - `/workspace/monitor.py` lines 85-105 (`STATE_HEARTBEAT_STALE_SEC` = 600, `STATE_PROMPT_STALE_SEC` = 90 and their comments)
    - `/workspace/monitor.py` lines 66-75 (`WORKING_LOCK_DIR`, `WORKING_LOCK_MAX_AGE_SEC` = 3600)
    - `/workspace/monitor.py` lines 1007-1023 (the stale-lock guard inside `scan()` that computes `working_locked` — the primitive to reuse, not to re-implement)
    - `/workspace/monitor.py` lines 1050-1080 (`scan()`'s row dict, confirming `working_locked`, `mtime`, `session_id`, `key` and the new `agent_activity` are all already present)
    - `/workspace/test_monitor.py` lines 1481-1570 (`Goal11_StateFileStaleness` — the class these behaviours extend)
    - `/workspace/.planning/phases/02-state-writer-shadow-mode/02-DIVERGENCE-REVIEW.md` (class 6 episode lengths, and carry-forward #1)
    - `/workspace/.planning/phases/02-state-writer-shadow-mode/02-UAT.md` lines 160-180 (the C1 Esc finding: legacy recovered at ~90s, the state engine at 600s)
  </read_first>
  <behavior>
    - Test 1 (D-02a): a WORKING state record with `last_event="PostToolBatch"` whose age is past 90s but well inside 600s, paired with a legacy entry for the same key carrying `working_locked=False` and a jsonl `mtime` newer than the state record's, recovers to WAITING.
    - Test 2 (D-02a guard): the same record with a legacy entry carrying `working_locked=True` stays WORKING until the normal 600s window elapses.
    - Test 3 (D-02a guard): the same record with NO matching legacy entry (hookless or jsonl-invisible session) stays WORKING until the normal 600s window — absence of evidence never triggers early recovery.
    - Test 4 (D-02b): a WORKING state record aged past 600s whose matching legacy entry has `agent_activity=True` stays WORKING.
    - Test 5 (D-02b): a WORKING state record aged past 600s whose matching legacy entry has `working_locked=True` stays WORKING.
    - Test 6 (D-02b): a WORKING state record aged past 600s with a matching legacy entry showing neither signal flips to WAITING, exactly as today.
    - Test 7 (precedence): the existing paused-container exception still suppresses the WAITING transition regardless of any fallback evidence.
  </behavior>
  <action>
Extend `scan_state_files()`'s single staleness-recovery block with the two named fallbacks. Both read evidence already computed once per tick by `scan()` — do not open, tail or parse any jsonl in `scan_state_files()`; do not add a second lock-file stat. RESEARCH.md's Don't-Hand-Roll table is explicit that a second jsonl reader is the wrong move.

Before the per-record loop, build a lookup from the `legacy_sessions` list keyed by each entry's `key` (and, where an entry carries a distinct `session_id`, by that too) so a state record can find its legacy counterpart in constant time. `scan()` already emits `working_locked`, `mtime` and `agent_activity` on every row.

**D-02a — Esc-interrupt early recovery.** Today a WORKING record only recovers once `age > window`. Add an earlier trigger: when the matching legacy entry exists AND reports `working_locked` False AND its jsonl `mtime` is newer than the state record's own `mtime`, recover to WAITING even though the heartbeat window has not elapsed. The two conjuncts matter. `working_locked` False alone is ambiguous — it is also the state before a turn ever starts. The jsonl-advance condition is what makes it an interrupt signal: on cc 2.1.220 the interrupt itself advances the transcript (UAT C1 measured this live), which is exactly what released legacy's stale-lock guard and produced the ~90s recovery the flip must not regress. Apply the same paused-container exception the existing branch applies, and apply the same status/dot/colour/rank assignment — this is an earlier path to the same WAITING verdict, never a different one. When no matching legacy entry exists, do nothing: a hookless or jsonl-invisible session must fall through to the normal window.

**D-02b — hook-silence pin.** Today a WORKING record past its window always degrades to WAITING unless the container is paused. Add a second exception: when the matching legacy entry reports `agent_activity` True OR `working_locked` True, suppress the transition and keep the record WORKING. This is divergence class 6 — long-running Monitor tool, foreground subagents, slow Bash — 9 events in the review week, with the longest episode at roughly 50 minutes, comfortably inside the 3600s working-lock ceiling that bounds `working_locked`. `agent_activity` is the Monitor/agent evidence 02-DIVERGENCE-REVIEW carry-forward #1 names, now sourced from the trimmed in-file peek of task 1 rather than from the deleted module.

Order the branches so D-02a's early recovery is evaluated before the `age > window` branch, and so D-02b's suppression sits alongside the existing paused check inside it. Both fallbacks must be inert whenever the matching legacy entry is missing.

Document both branches in the function docstring as the two named survivors from the hybrid verdict, naming which TEST-MATRIX case and which divergence class each covers, so a future reader does not mistake them for incidental complexity.

Add the seven cases above to `Goal11_StateFileStaleness`, passing hand-built `legacy_sessions` lists rather than exercising `scan()` — these are unit tests of the fallback wiring, and the peek and lock computation have their own coverage in task 1.
  </action>
  <verify>
    <automated>cd /workspace && python3 -m unittest test_monitor.py 2>&1 | tail -3 && python3 -m unittest test_monitor.Goal11_StateFileStaleness -v 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -m unittest test_monitor.Goal11_StateFileStaleness` reports OK and its test count is at least 7 higher than the pre-task count recorded in the SUMMARY.
    - Test 1 passes: a WORKING record aged between `STATE_PROMPT_STALE_SEC` and `STATE_HEARTBEAT_STALE_SEC` with a legacy counterpart carrying `working_locked=False` and a newer `mtime` yields `status == "WAITING"`.
    - Test 2 and Test 3 pass: the same record with `working_locked=True`, and with no legacy counterpart at all, both yield `status == "WORKING"`.
    - Test 4 and Test 5 pass: a record aged past `STATE_HEARTBEAT_STALE_SEC` with a legacy counterpart carrying `agent_activity=True` (or `working_locked=True`) yields `status == "WORKING"`.
    - Test 6 passes: the same aged record with a legacy counterpart showing neither signal yields `status == "WAITING"`.
    - Test 7 passes: a `hostname_to_status` entry of `"paused"` keeps the record WORKING irrespective of the fallback evidence.
    - `scan_state_files()` opens no `.jsonl` file and performs no `WORKING_LOCK_DIR` stat: a test that monkeypatches `monitor.PROJECTS_DIR` and `monitor.WORKING_LOCK_DIR` to non-existent paths, passes an explicit `legacy_sessions` list, and still gets the pinned/recovered verdicts, proves the evidence came from the passed-in list.
    - The `scan_state_files()` docstring names both fallbacks, their TEST-MATRIX case numbers and their divergence class.
  </acceptance_criteria>
  <done>Esc-interrupted turns recover in ~90s and hook-silent long tools stay pinned WORKING, both driven purely by evidence `scan()` already computes, with seven new staleness cases green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: SessionEnd tombstones and the stale temp-file sweep</name>
  <reversibility rating="reversible">The tombstone is an extra marker file written after the existing removal; stopping writing and reading it restores today's behaviour, and orphaned markers age out through the same prune that removes state files.</reversibility>
  <precondition>`hooks/state-writer.sh` still validates `session_id` against its filename allowlist before any path is built — the tombstone write must sit after that guard, never before it.</precondition>
  <files>monitor.py, hooks/state-writer.sh, test_monitor.py, test_state_writer.py</files>
  <read_first>
    - `/workspace/hooks/state-writer.sh` (full file — the header mapping comment, the session_id allowlist guard, the SessionEnd branch that removes the state file, and the atomic tmp+rename writer at the end)
    - `/workspace/monitor.py` lines 630-715 (`scan_state_files()`'s directory listing, the `STATE_PRUNE_AGE_SEC` prune loop, and the legacy-merge tail that carries `legacy_origin` rows)
    - `/workspace/monitor.py` lines 76-105 (`STATE_DIR`, `STATE_PRUNE_AGE_SEC`, `MAX_AGE_SEC`)
    - `/workspace/test_monitor.py` lines 1323-1360 (`StateFileTestBase` and `_write_state`, which the new fixtures extend)
    - `/workspace/test_monitor.py` lines 1586-1675 (`Goal13_HooklessFallback` — the bridge these tombstones gate)
    - `/workspace/test_state_writer.py` lines 120-200 (the `_HomeTestCase` harness and how it invokes the hook with a payload)
    - `/workspace/test_state_writer.py` lines 394-520 (the event-mapping test family, including the SessionEnd cases)
  </read_first>
  <behavior>
    - Test 1: invoking the hook with a `SessionEnd` payload removes the state file AND leaves a tombstone marker named for the same session id.
    - Test 2: a `session_id` containing a path separator or a dot-dot segment produces neither a state file, nor a tombstone, nor any write outside the state directory.
    - Test 3: `scan_state_files()` given a legacy session whose key matches a tombstone does NOT carry that session through the bridge.
    - Test 4: `scan_state_files()` given a legacy session with no tombstone still carries it through with `legacy_origin` True (the D-02c bridge is unaffected).
    - Test 5: a tombstone older than `STATE_PRUNE_AGE_SEC` is unlinked by the prune loop; one younger than that is kept.
    - Test 6: a temp file matching the atomic-writer's naming pattern, aged past `STATE_PRUNE_AGE_SEC`, is unlinked; a freshly-created one is left untouched.
    - Test 7: a tombstone file never appears as a session row (it is not picked up by the state-record glob).
  </behavior>
  <action>
Close D-06's ghost-row outcome and D-08's orphan-temp-file cleanup. Both changes land in the same directory-scanning region of `scan_state_files()`, plus one branch in the hook.

**Mechanism decision for D-06 (this is the discretionary choice CONTEXT.md defers).** Use an on-disk tombstone marker rather than an in-process set. The state writer already removes the state file on SessionEnd, so nothing durable records that the session ended; an in-memory set would forget every tombstone on monitor restart and the ghost would return, which fails D-06's locked outcome. Introduce a `STATE_TOMBSTONE_SUFFIX` module constant in `monitor.py` and have the hook create a zero-byte marker at the state directory path formed from the session id plus that suffix. Choose a suffix that cannot collide with the `*.json` glob the record loop uses.

**Hook change.** In `hooks/state-writer.sh`'s SessionEnd branch, keep the unconditional state-file removal exactly as it is (it covers every `reason` value — cases 10/11/14) and add the tombstone creation immediately after it. The branch must sit after the existing `session_id` allowlist guard that rejects any character outside the permitted filename set, so the tombstone path is subject to the same validation as the state-file path; do not introduce a second, weaker path build. Update the header's event-to-state mapping comment so the SessionEnd line describes both effects.

**Monitor change — bridge suppression.** In `scan_state_files()`, before the legacy-merge tail, glob the state directory for tombstone markers and collect their session ids into a set. In the merge loop, skip any legacy entry whose `key` or `session_id` is in that set, instead of carrying it through as a `legacy_origin` row. Everything else about the bridge is unchanged: an entry with no tombstone is still carried verbatim with the `legacy_origin` marker (D-02c / ENG-05). A session that ends and is later resumed writes a fresh state file, which takes precedence because the state-record loop runs first and its keys are already excluded from the merge — but delete the tombstone when a live state record exists for the same session id, so a resumed session cannot be suppressed by its own stale marker.

**Monitor change — sweeps.** Extend the existing age-based prune so it also removes tombstones older than `STATE_PRUNE_AGE_SEC`, and sweeps leftover atomic-write temp files matching the writer's temp naming pattern that are older than the same threshold. Reuse `STATE_PRUNE_AGE_SEC` rather than introducing a new constant: a temp file that old is unambiguously orphaned, since tmp-then-rename completes in milliseconds, and the wide margin is what guarantees the sweep can never destroy a write that is genuinely in flight. Both sweeps must be best-effort — an OSError on one file must not abort the tick, matching the existing prune's behaviour. This is the code half of D-08's cleanup; the one known orphan on the Windows machine is removed by this sweep on the first tick after the flip, not by a manual delete.

**Tests.** Add Tests 1 and 2 to `test_state_writer.py`'s event-mapping family, and Tests 3 through 7 to `test_monitor.py` alongside `Goal13_HooklessFallback` and the `StateFileTestBase` family.
  </action>
  <verify>
    <automated>cd /workspace && python3 -m unittest test_monitor.py test_state_writer.py test_event_logger.py 2>&1 | tail -3 && bash -n hooks/state-writer.sh && echo "SHELL SYNTAX OK"</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n hooks/state-writer.sh` exits 0.
    - `python3 -m unittest test_monitor.py test_state_writer.py test_event_logger.py` reports OK.
    - Feeding the hook a `SessionEnd` payload for session id `abc123` leaves no `abc123.json` in the state directory and leaves exactly one tombstone file whose name begins with `abc123`.
    - Feeding the hook a `SessionEnd` payload whose `session_id` is `../evil` creates no file anywhere: the state directory listing is unchanged and no file named `evil` exists in its parent.
    - `scan_state_files(legacy_sessions=[{...key: "s1"...}])` with a tombstone present for `s1` returns a list containing no row with `key == "s1"`.
    - The same call with no tombstone returns a row for `s1` whose `legacy_origin` is True.
    - A tombstone with an mtime older than `STATE_PRUNE_AGE_SEC` is absent from disk after a `scan_state_files()` call; one with a current mtime is still present.
    - A file matching the temp-write naming pattern with an mtime older than `STATE_PRUNE_AGE_SEC` is absent after a `scan_state_files()` call; a freshly created one is still present.
    - No tombstone or temp file ever appears in the returned session list (assert no row's `key` matches a tombstone or temp filename stem).
    - D-11 door-keeping holds: a record written by any state-writing event still contains a non-empty `hostname` field; every path this task writes or sweeps resolves inside the state directory; and no code added here reads state from, or writes state to, a host other than the local machine.
  </acceptance_criteria>
  <done>Cleanly-ended sessions stay gone across monitor restarts, the D-02c bridge still carries genuinely hookless sessions, and orphaned atomic-write temp files sweep themselves.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Claude Code hook payload (JSON on stdin) → `hooks/state-writer.sh` → filesystem path | `session_id` from an untrusted payload is used to build a filename for both the state file and the new tombstone |
| `~/.claude/projects/*.jsonl` → `monitor.py` activity peek | Transcript content written inside containers is pattern-matched on the host |
| `~/.claude/monitor-state/` → `monitor.py` sweeps | The monitor now unlinks files in a directory that container-side hooks write concurrently |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-02 | Tampering | tombstone path built from `session_id` in `hooks/state-writer.sh` SessionEnd branch | medium | mitigate | Task 3 places the tombstone write strictly after the script's existing `session_id` character allowlist, reusing that single validated path build; Test 2 asserts a dot-dot session id writes nothing |
| T-03-03 | Denial of Service | unbounded `*.json.tmp.*` accumulation on the shared 9p mount from interrupted atomic writes | low | mitigate | Task 3 adds an age-based sweep to the existing prune loop, thresholded at `STATE_PRUNE_AGE_SEC` so an in-flight write is never a candidate |
| T-03-06 | Spoofing | a ghost jsonl row for a killed/ended session claiming a live session's identity through the `legacy_origin` bridge | low | mitigate | Task 3's tombstone suppresses bridging for any session the state engine saw end; the pre-existing stale-sessionId drop in `scan()` remains the first line |
| T-03-09 | Denial of Service | the activity peek reading a trailing window of every candidate jsonl each tick | low | mitigate | Task 1 collapses three whole-file reads per session into one, strictly reducing per-tick I/O; the OSError path returns the safe default rather than raising |
| T-03-04 | Information Disclosure | the activity peek's pattern matches over raw transcript bytes | medium | mitigate | The peek returns a single boolean and retains no matched text; no captured group is stored, logged or written to any file — the flagged prohibition forbids a successor that does |
| T-03-SC | Tampering | npm/pip/cargo installs | high | accept | No package-manager install task exists in this phase; Python 3.10+ stdlib plus bash/jq only, per the project's zero-dependency constraint and RESEARCH.md's not-applicable audit |
</threat_model>

<verification>
- All three unit suites green in one process and individually.
- `shell_tracker.py` absent from the tree and unimportable.
- One badge per row/chip, sourced only from the hook-captured count.
- The two fallbacks provably read passed-in legacy evidence, not fresh file I/O.
- Tombstones suppress ghosts; temp files sweep; neither ever renders as a session.
</verification>

<success_criteria>
- FLIP-01's engine work is complete: `scan()` serves exactly the three named fallbacks and nothing else.
- Divergence class 3 (bg-shell rule mismatch) is gone by construction on the monitor side.
- Divergence class 6 (hook silence) is covered by the pin instead of producing false notifications.
- Cleanly-ended sessions stay gone across a monitor restart.
</success_criteria>

<flagged_assumptions>
- **Edge probe FLIP-03 / unclassified (`unresolved`)** — carried forward from plan 03-01; resolved documentally in plan 03-04, not here.
- **Canon-referral breadcrumb:** path-traversal on session_id-derived filenames is canon security — covered by `/gsd-secure-phase` and threat rows `T-03-01`/`T-03-02`; not minted as a prohibition.
</flagged_assumptions>

<output>
Create `.planning/phases/03-flip-to-default-cleanup/03-02-SUMMARY.md` when done. Record the chosen tombstone suffix, the pre/post `Goal11_StateFileStaleness` test counts, and the post-task `wc -l monitor.py`.
</output>
