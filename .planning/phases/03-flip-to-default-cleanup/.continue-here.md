---
context: phase
phase: 03-flip-to-default-cleanup
task: complete
total_tasks: 4
status: awaiting_live_uat
last_updated: 2026-08-06T13:16:07.168Z
---

# Machine handoff — resume on the WINDOWS machine for the flip UAT

Phase 3 code work is DONE (4/4 plans, review+fix cycle closed, 21/21 must-haves verified, 236
tests green). This handoff exists because the ONLY remaining work — the live UAT of the flip —
runs on the Windows machine, where shadow mode and the real sessions live (locked decision D-12).
Symmetric to the Phase 2→3 devbox handoff, in the opposite direction.

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Review fix contradicting a locked decision | The WR-02 code-review fix folded auq-lock removal into the default install path, silently defeating D-04's Section-H parity gate. Caught by the phase verifier with a repro, then corrected | advisory | Goal-backward verification re-checks fixes against 03-CONTEXT.md locked decisions — keep running the verifier after any fix pass; never accept a fix that touches install/teardown sequencing without re-reading D-04 |
| Harness worktree fork-base divergence (#683) | On this unpushed branch, `Agent(isolation="worktree")` forked from a stale remote base; the executor's fail-closed guard caught it | advisory | Executors ran sequentially on the main tree this session; if resuming with worktrees on an unpushed branch, set `worktree.baseRef: "head"` first |

<current_state>
- All Phase 3 commits are on `feat/hook-refactor` (HEAD at handoff: 8080b53). Working tree clean.
- 03-VERIFICATION.md: status `human_needed`, 21/21 must-haves — the human items are the D-12
  live-machine ones (full 03-UAT.md run, WR-01/WR-04 live confirmation, TEST-MATRIX rows 19-21).
- The phase is NOT marked complete in ROADMAP/STATE — `/gsd-verify-work 3` does that after UAT.
</current_state>

<completed_work>
- 03-01: render seam flipped to the state-file engine; shadow machinery + `project_name()` deleted.
- 03-02: three hybrid fallbacks (D-02a/b/c), SessionEnd tombstones, stale-tmp sweep, `shell_tracker.py` deleted, unified ◉N badge.
- 03-03: `Stop` badge-only at the source, auq-lock retired from the installer (+ `--remove-auq-lock`), working-lock allowlist guard, FLIP-02 equivalent coverage (236 vs 205 baseline).
- 03-04: README rewritten, `docs/TEST-MATRIX.md` (git mv, history kept) + `docs/RECERTIFICATION.md`, 03-UAT.md runbook, COVERAGE.md.
- Review cycle: CR-01 + WR-01..05 fixed (03-REVIEW.md, 03-REVIEW-FIX.md); WR-02 corrected after verifier repro.
</completed_work>

<remaining_work>
- Run `03-UAT.md` on the Windows machine: Setup → Sections A–I → Teardown (Teardown ONLY after Section H passes).
- `/gsd-verify-work 3` to record results and close the phase.
- Optional: `/gsd-secure-phase 3` (security enforcement on, no SECURITY.md yet).
</remaining_work>

<decisions_made>
- WR-02 corrected: plain `bash hooks/install.sh` PRESERVES pre-flip auq-lock entries (informational note only); `--remove-auq-lock` is the sole, Section-H-gated removal path (D-04).
- D-02b pin keeps Monitor/agent evidence via a trimmed in-file peek; the shell_tracker module still died (resolved RESEARCH Open Q1).
- `--state-files` is an accepted no-op; rollback story is `git revert`, no `--legacy` hatch.
</decisions_made>

<blockers>
- None. (Everything left is deliberate live-machine work with an existing runbook.)
</blockers>

## Required Reading (in order)
1. `03-UAT.md` (this directory) — the live validation runbook: Setup, Sections A–I, gated Teardown
2. `03-VERIFICATION.md` — the three human_verification items (incl. WR-01/WR-04 live confirmation)
3. `03-REVIEW-FIX.md` — what the fix cycle changed and why (esp. the WR-02 correction)
4. `docs/RECERTIFICATION.md` §"Upgrading from a pre-flip install" — what plain install now does on the live machine

## Infrastructure State
- Windows machine (pre-pull): shadow mode ACTIVE with the pre-flip hook set (state-writer + working-lock + auq-lock + event-logger); divergence log still accumulating. Pulling the branch + `bash hooks/install.sh` upgrades hooks but PRESERVES auq-lock entries until the gated teardown.
- `~/.claude/monitor-state/` is the primary data source post-flip — never `rm -rf` it as "cleanup"; only the divergence log and orphan `.tmp` files go (per 03-UAT.md Teardown).
- Devbox: work finished; nothing running; repo artifacts self-sufficient by design.

<context>
The flip is real now: the monitor renders exclusively from hook-written state files, with exactly
three legacy consumers left (Esc recovery, hook-silence pin, hookless bridge). The UAT proves it
on live sessions and then — gated on needs_input parity — retires auq-lock from the live install
and tears down the shadow-era artifacts. After that, v1.0's milestone close and the remote-devbox
milestone are next.
</context>

<next_action>
Start with (on the Windows machine, after pulling feat/hook-refactor): `/gsd-resume-work` — it
will load this handoff and route to the 03-UAT.md run, then `/gsd-verify-work 3`.
</next_action>
